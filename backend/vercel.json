{
	"installCommand": "pip install uv && uv sync",
	"buildCommand": "python -c \"from main import app; print('App loaded:', getattr(app, 'title', 'Voice Agent API'))\"",
	"framework": null,
	"outputDirectory": null
}
